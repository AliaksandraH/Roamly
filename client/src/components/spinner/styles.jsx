import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: "100%",
        height: "100%",
        top: 0,
        left: 0,
        backgroundColor: "rgba(180, 201, 219, 0.5)",
        position: "absolute",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        zIndex: 100,
    },
});

export default styles;
