import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    filter: {
        marginBottom: 10,
    },
    filterLabel: {
        fontSize: 20,
    },
});

export default styles;
